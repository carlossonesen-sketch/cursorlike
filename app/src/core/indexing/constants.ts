/**
 * Workspace cache paths for indexing (embeddings + reranker later).
 * Index stored at <workspace>/.cursorlite/index
 */

export const CURSORLITE_INDEX_DIR = ".cursorlite/index";
export const CURSORLITE_MODELS_DIR = ".cursorlite/models";
